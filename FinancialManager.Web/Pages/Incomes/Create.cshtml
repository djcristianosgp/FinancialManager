@page
@model FinancialManager.Web.Pages.Incomes.CreateModel
@{
    ViewData["Title"] = "Nova Receita";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="mb-4">
    <h1>💰 Nova Receita</h1>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/incomes">Receitas</a></li>
            <li class="breadcrumb-item active">Nova</li>
        </ol>
    </nav>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <form method="post">
                    <div class="mb-3">
                        <label class="form-label">Descrição</label>
                        <input type="text" class="form-control" asp-for="Input.Description" required />
                        <span asp-validation-for="Input.Description" class="text-danger"></span>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Valor</label>
                            <input type="number" step="0.01" class="form-control" asp-for="Input.Amount" required />
                            <span asp-validation-for="Input.Amount" class="text-danger"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">Data</label>
                            <input type="date" class="form-control" asp-for="Input.Date" required />
                            <span asp-validation-for="Input.Date" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Categoria</label>
                        <input type="text" class="form-control" asp-for="Input.Category" placeholder="Ex: Salário, Freelance, Investimentos" required />
                        <span asp-validation-for="Input.Category" class="text-danger"></span>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">Conta Bancária</label>
                            <select class="form-select" asp-for="Input.BankAccountId" required>
                                <option value="">Selecione...</option>
                                @foreach (var account in Model.BankAccounts)
                                {
                                    <option value="@account.Id">@account.Bank - @account.Type</option>
                                }
                            </select>
                            <span asp-validation-for="Input.BankAccountId" class="text-danger"></span>
                        </div>

                        <div class="col-md-6 mb-3">
                            <label class="form-label">Recorrência</label>
                            <select class="form-select" asp-for="Input.Recurrence" required>
                                <option value="0">Única</option>
                                <option value="1">Mensal</option>
                                <option value="2">Anual</option>
                            </select>
                            <span asp-validation-for="Input.Recurrence" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success">Salvar</button>
                        <a href="/incomes" class="btn btn-secondary">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
